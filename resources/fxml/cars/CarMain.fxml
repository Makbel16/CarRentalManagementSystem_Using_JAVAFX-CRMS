<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>
<?import javafx.geometry.Insets?>

<ScrollPane fitToWidth="true"
            xmlns="http://javafx.com/javafx/11.0.1"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="controllers.cars.CarMainController"
            stylesheets="@../../css/style.css">
   
   <content>
      <!-- OPTION 1: Relative path from bin folder -->
      <VBox spacing="20.0" styleClass="main-container" 
            style="-fx-background-image: url('images/car-background.jpg');
                   -fx-background-size: cover;
                   -fx-background-position: center;
                   -fx-background-repeat: no-repeat;">
         
      <!-- OPTION 2: Absolute path (replace with your actual path) -->
      <!-- <VBox spacing="20.0" styleClass="main-container" 
            style="-fx-background-image: url('file:/C:/Users/ybeka/eclipse-workspace/CarRentalManagementSystem/images/car-background.jpg');
                   -fx-background-size: cover;
                   -fx-background-position: center;
                   -fx-background-repeat: no-repeat;"> -->
      
      <!-- OPTION 3: From resources folder -->
      <!-- <VBox spacing="20.0" styleClass="main-container" 
            style="-fx-background-image: url('@../../images/car-background.jpg');
                   -fx-background-size: cover;
                   -fx-background-position: center;
                   -fx-background-repeat: no-repeat;"> -->

         <padding>
            <Insets bottom="20.0" left="20.0" right="20.0" top="20.0" />
         </padding>
         
            <Label text="Car Management" styleClass="page-title"
                alignment="CENTER"
                maxWidth="Infinity"
                style="-fx-text-fill: white; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.7), 3, 0, 0, 1);">
            <font>
               <Font name="System Bold" size="28.0" />
            </font>
        </Label>
         
         <!-- Buttons Row with Images -->
         <HBox spacing="20" alignment="CENTER">
            <!-- Add Car Button -->
            <Button id="addCarBtn"
                    text="➕ Add Car" 
                    onAction="#handleAddCar" 
                    styleClass="car-icon-button" 
                    minWidth="180"
                    minHeight="160" />
            
            <!-- Delete Car Button -->
            <Button id="deleteCarBtn"
                    text="🗑️ Delete Car" 
                    onAction="#handleDeleteCar" 
                    styleClass="car-icon-button" 
                    minWidth="180"
                    minHeight="160" />
            
            <!-- Edit Car Button -->
            <Button id="editCarBtn"
                    text="✏️ Edit Car" 
                    onAction="#handleEditCar" 
                    styleClass="car-icon-button" 
                    minWidth="180"
                    minHeight="160" />
            
            <!-- Search Car Button -->
            <Button id="searchCarBtn"
                    text="🔍 Search Car" 
                    onAction="#handleSearchCar" 
                    styleClass="car-icon-button" 
                    minWidth="180"
                    minHeight="160" />
            
            <!-- View All Cars Button -->
            <Button id="viewAllCarsBtn"
                    text="🔍 View All Cars" 
                    onAction="#handleViewAllCars" 
                    styleClass="car-icon-button" 
                    minWidth="180"
                    minHeight="160" />
         </HBox>
         
         <!-- Content Pane -->
         <StackPane fx:id="contentPane" VBox.vgrow="ALWAYS" style="-fx-padding: 20 0 0 0;">
            <!-- Content will be loaded here -->
         </StackPane>
      </VBox>
   </content>
</ScrollPane>